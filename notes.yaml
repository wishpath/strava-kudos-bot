PROBLEM:
  - either clicking or not clicking, the entry title should be visible on top of the screen, in case of kudos is already clicked, scroll down gradually gradually to see it, dont just teleport
  - scroll_to_bottom_of_page - can those waits be reduced/stopped by some trigger, like "page fully loaded state".
  - self.get_owners_name(feed_entry, kudos_button, single_entry_kudos_buttons_count): in case of multiple kudos, is it correct to pass the whole feed_entry?
  - collect who liked my entries

LETS NOT:
  - research more on loading more entries by url? example https://www.strava.com/dashboard?num_entries=150 (not working)